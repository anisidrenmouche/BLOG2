{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron">
        <div class="container">
            <div class="row">
                    <div class="col-md-8">

                    </div>
                    <div class="col-md-4">
                        <h1>{{ property.title }}</h1>
                        <h2>{{ property.rooms }} piéces - {{ property.surface }}m</h2>
                        <div class="text-primary" style="font-size: 4rem;font-weight: bold";>{{ property.price }}€</div>
                        <a href="#" class="btn btn-primary">Contacter l'agence </a>
                    </div>
                </div>

            </div>
        </div>
    </div>
<div class="container mt-4">

    <p>
        {{ property.desciption | nl2br }}
    </p>
    <div class="row">
        <div class="col-md-8">
            <h4>Caracteristques </h4>
            <table class="table table-striped">



                    <tr>
                    <td>Surface habitable</td>
                 <td>{{ property.surface }}</td>
                </tr>

                 <tr>
                <td>Piéces</td>
                <td>{{ property.rooms }}</td>
                </tr>

             <tr>
                <td>Piéces</td>
                <td>{{ property.bedrooms }}</td>
            </tr>

                <tr>
                <td>Etages</td>
                <td>{{ property.floor }}</td>
            </tr>

                <tr>
                <td>chauffage</td>
                <td>{{ property.heatType }}</td>
            </tr>
            </table>
        </div>
        <div class="col-md-4">
            <h2>Specificites</h2>
        </div>

        </div>
    </div>
{% endblock %}
